"use strict";

trigonometFunc = {
  description: "Trigonometric functions",

  sin(n, fi) {
    let rezult = 0;
    return rezult = Math.sin(n*fi);
  },
  cos(n, fi) {
    let rezult = 0;
    return rezult = Math.cos(n*fi);
  }
};